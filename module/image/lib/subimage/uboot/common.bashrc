# Bash script: subimage: fastboot: common

# Directories
outdir_fastboot_release=${CONFIG_SYNA_SDK_OUT_TARGET_PATH}/${CONFIG_UBOOT_REL_PATH}

# Directory for preboot.subimg
outfile_preboot_subimg=${outdir_subimg_intermediate}/preboot.subimg

# Output file
outfile_fastboot_subimg=${outdir_subimg_intermediate}/fastboot.subimg

# Output file for preboot with fastboot
outfile_preboot_fastboot_subimg=${outdir_subimg_intermediate}/preboot_fastboot.subimg

