# Bash script: subimage: fastlogo: common

# Directories
workdir_security_keys=${CONFIG_SYNA_SDK_OUT_TARGET_PATH}/${CONFIG_SECURITY_KEY_PATH}/${syna_chip_name}/${syna_chip_rev}

# Output file
outfile_fastlogo_subimg=${outdir_subimg_intermediate}/fastlogo.subimg
