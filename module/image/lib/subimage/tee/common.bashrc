# Bash script: subimage: preboot: common

# Directories
outdir_tee_release=${CONFIG_SYNA_SDK_OUT_TARGET_PATH}/${CONFIG_TEE_KERNEL_REL_PATH}

# Output file
outfile_tee_subimg=${outdir_subimg_intermediate}/tee.subimg
outfile_tee_recovery_subimg=${outdir_subimg_intermediate}/tee_recovery.subimg
