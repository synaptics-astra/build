# Bash script: subimage: preboot: common

# Directories
outdir_preboot_release=${CONFIG_SYNA_SDK_OUT_TARGET_PATH}/${CONFIG_PREBOOT_REL_PATH}

# Output file
outfile_preboot_subimg=${outdir_subimg_intermediate}/preboot.subimg
